meta {
  name: List Sync Configs
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/admin/sync-configs?client_id={{clientId}}
  body: none
  auth: none
}

params:query {
  client_id: {{clientId}}
}

assert {
  res.status: eq 200
}
