meta {
  name: Delete Sync Config
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/api/v1/admin/sync-configs/{{configId}}
  body: none
  auth: none
}

assert {
  res.status: eq 204
}

docs {
  Cleans up the sync config created earlier in this folder.
}
